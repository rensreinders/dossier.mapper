apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  revisionHistoryLimit: 1
  minReadySeconds: 20
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 10001
      containers:
        - name: app
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: IfNotPresent
          # Laat de CMD van het image staan (php -S 0.0.0.0:8080 index.php)
          workingDir: /app
          ports:
            - name: http
              containerPort: 8080
          # Resources: kleine, veilige default zonder values-afhankelijkheid
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
          # Laad alle key/values van de ConfigMap (en optioneel Secret) als env-vars
          envFrom:
            - configMapRef:
                name: {{ .Values.configMapName }}
      imagePullSecrets:
        - name: regcred
      terminationGracePeriodSeconds: 30
